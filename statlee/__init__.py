"""STATlee application package.

The Flask app factory and its focused modules (config, storage, sandbox, llm,
prompts, datatools, models, billing, extensions) plus the ``routes`` blueprints
and bundled ``static``/``templates`` assets live here. The WSGI entry point is
``wsgi:app`` at the repository root.
"""
