# Backlog — parked ideas

New ideas raised mid-run land here so the frozen scope keeps converging. Brainstorm these as the
*next* autopilot cycle, not this one.

## Inbox (unsorted ideas)
- Resolve the `main` ↔ branch divergence: main still has the Anthropic multi-provider commits that
  were deliberately removed on the Gemini-only line. Decide whether to force main to the Gemini-only
  history or keep multi-provider. (Human decision; flagged in memory as "repo/Render rename deferred".)
- Repo/Render rename CodeCaster→STATlee and redeploy (deferred from prior cycle, per memory).
- When/if monetizing for real: wire Stripe Checkout + webhook to credit `User.credits` (the billing
  seam is ready; this is the "real money + secrets" step that must stay a human action).
- Make the global `monthly_priority_call_ceiling` truly global (shared store) instead of per-process.

## Next cycle (promoted, ready to brainstorm)
-

## Done / shipped
-
