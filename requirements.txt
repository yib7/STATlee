# Pinned direct dependencies (roadmap 2.2). For a fully-resolved lockfile,
# compile with pip-tools: `pip-compile requirements.in`.
Flask==3.1.3
Werkzeug==3.1.8
pandas==3.0.1
google-genai==2.12.1
anthropic==0.117.0    # optional provider (LLM_PROVIDER=anthropic); lazy-imported
openai==2.45.0        # optional provider (LLM_PROVIDER=openai); lazy-imported
statsmodels==0.14.6
matplotlib==3.11.0
seaborn==0.13.2
python-dotenv==1.2.2
filelock==3.30.2      # cross-worker lock on the dataset version manifest (P2-9)
gunicorn==26.0.0
fpdf2==2.8.7
pypdf==6.13.3        # >=6.13.3 fixes 8 CVEs in the PDF parser (untrusted uploads)
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0    # Alembic migrations wired into the app factory (boot upgrade)
alembic==1.18.5         # pulled in by Flask-Migrate; pinned here since boot migrations depend on it
Flask-Login==0.6.3
Flask-Limiter==4.1.1
redis==8.0.1          # shared rate-limit store (RATELIMIT_STORAGE_URI=redis://)
openpyxl==3.1.5
xlrd==2.0.2           # .xls engine; openpyxl only covers .xlsx
pyreadstat==1.3.5
